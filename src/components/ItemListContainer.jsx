/* const ItemListContainer = (props) => {
    return(
        <h1>{props.message}</h1>
    )
}

export default ItemListContainer */

const ItemListContainer = ({message}) => {
    return(
        <h1>{message}</h1>
    )
}

export default ItemListContainer